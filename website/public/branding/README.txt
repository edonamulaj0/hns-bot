Branding files (website/public/branding/)

  hns-name.png   — Navbar logo (`navbar.tsx` + `lib/branding.ts`). Transparent background recommended; shown ~32–40px tall CSS.

Favicon (browser tab):

  website/public/icon.svg — Used as site icon (see app/layout.tsx metadata). Replace this file, OR add public/favicon.ico and update metadata.
