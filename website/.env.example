# Public site URL (no trailing slash). Used for auth/session fetch and absolute links.
# Example: https://hns.gg
NEXT_PUBLIC_BASE_URL=

# Bot / API Worker origin for server-side fetch and optional browser rewrites.
# Example: https://api.hns.gg  or  https://hns-bot.your-subdomain.workers.dev
# In development, if unset or placeholder, some API calls return empty data (production)
# or throw if NEXT_PUBLIC_API_URL contains YOUR_SUBDOMAIN (development).
NEXT_PUBLIC_API_URL=

# Same as HNS_WORKER_URL for Vercel/CI; Next prefers this for server fetches when set.
HNS_WORKER_URL=

# Auth Worker origin (separate wrangler project in /auth). Required for /auth/login rewrites in production.
# Local: run `cd auth && npx wrangler dev --port 8788` and leave unset to use default http://127.0.0.1:8788 in dev.
HNS_AUTH_WORKER_URL=

# Discord server id (snowflake). Required for the site widget iframe; enable the widget in Discord:
# Server Settings → Engagement → Server Widget → ON, and pick an invite channel.
# Example guild (H4cknStack): widget https://discord.com/widget?id=1387695542934437979&theme=dark
NEXT_PUBLIC_DISCORD_GUILD_ID=1387695542934437979

# OAuth application client id (public). Shown in UI; auth flow uses the auth Worker.
NEXT_PUBLIC_DISCORD_CLIENT_ID=
